{
  "name": "copilot-chat-pkm-exporter",
  "publisher": "copilot-pkm-bridge",
  "displayName": "Copilot Chat PKM Exporter",
  "description": "Exports local GitHub Copilot JSONL chat history to structured Markdown.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilot-pkm-bridge.exportChat",
        "title": "Copilot PKM: Export Chat (All Workspaces)",
        "category": "Copilot PKM"
      },
      {
        "command": "copilot-pkm-bridge.exportChatCurrent",
        "title": "Copilot PKM: Export Chat (Current Workspace)",
        "category": "Copilot PKM"
      }
    ],
    "configuration": {
      "title": "Copilot PKM Bridge",
      "properties": {
        "copilot-pkm-bridge.defaultExportFolder": {
          "type": "string",
          "default": "${workspaceFolder}/.docs/ai-logs",
          "description": "Default folder to save exported chats. Supports ${workspaceFolder} and ${userHome}."
        },
        "copilot-pkm-bridge.includeMetadata": {
          "type": "boolean",
          "default": true,
          "description": "Include metadata (model, date, etc.) in the exported Markdown."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.109.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.54.0",
    "eslint": "^9.39.2",
    "esbuild": "^0.27.2",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
